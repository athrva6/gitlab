jobs:
  test:
   runs-on: ubuntu-latest
   steps:
    -uses: actions/checkout@v4
    - run: echo "Running tests"
  needs: test
  runs-on: ubuntu-latest
  steps:
   - uses: actions/checkout@v4
   - run: echo "deploying,because"
